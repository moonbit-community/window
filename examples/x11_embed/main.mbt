///|
// Port target: winit-reference/winit/examples/x11_embed.rs (non-x11 branch)
fn main {
  println("This example is only supported on X11 platforms.")
  println(
    "The current MoonBit port targets macOS AppKit only, so X11 embedding is unavailable.",
  )
}
